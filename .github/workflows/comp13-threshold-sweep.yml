name: COMP-1.3 Threshold Sweep
on:
  push:
    branches: [balance/comp-1.3-candidate]
jobs:
  sweep:
    strategy:
      fail-fast: false
      matrix:
        offset: [0,30,60,90,120,150,180,210,240,270]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm run preflight
      - name: Sweep readiness thresholds across 30 policies
        env:
          OFFSET: ${{ matrix.offset }}
          COUNT: 30
        run: node scripts/comp13-threshold-sweep.mjs
